# Nome dell'applicazione
spring.application.name=demo_ecommerce

# Configurazione del database PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=1234


logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE


# La chiave segreta per firmare i token JWT
jwt.secret=EMgxFtFxSIlaPBl2sH/+KQg/XLREN/VnJOQSfZphk7IE/0GhwzMLbGsCd30vTko8pk83sn0WJ0NK7vNNcpuNPw==


# Scadenza del token in millisecondi (es. 1 ora)
jwt.expiration=3600000
firebase.storage.bucket=unilire.firebasestorage.app






# Configurazione JPA e Hibernate
spring.jpa.open-in-view=false
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


# Livello di logging
logging.level.demo.demo_ecommerce=INFO
logging.level.org.keycloak=DEBUG


spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8081/realms/MIO_REALM
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${spring.security.oauth2.resourceserver.jwt.issuer-uri}/protocol/openid-connect/certs

jwt.auth.converter.resource-id=MIO_REALM
jwt.auth.converter.principle-attribute=preferred_username

## logging
logging.level.org.springframework.security=INFO
logging.pattern.console=%d{dd-MM-yyyy HH:mm:ss} %magenta([%thread]) %highlight(%-5level) %logger.%M - %msg%n



spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=587
spring.mail.username=2f3c224633f50a
spring.mail.password=a3c4ee00744331
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.main.allow-bean-definition-overriding=true




# Server Port
server.port=8080

# Active Profile
spring.profiles.active=dev

# Actuator: esponi endpoint per monitoring
management.endpoints.web.exposure.include=health,info,metrics,caches,loggers
management.endpoint.metrics.enabled=true
management.endpoint.caches.enabled=true

# Abilita tutte le metriche
management.metrics.enable.all=true

# Prefisso path Actuator (opzionale)
management.endpoints.web.base-path=/actuator
